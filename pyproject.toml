[project]
name = "agent-router"
version = "0.2.2"
description = "Local-first workflow/specialist/model-tier router for MCP-capable coding agents"
requires-python = ">=3.9"
dependencies = []

[project.scripts]
agent-router = "server:main"
